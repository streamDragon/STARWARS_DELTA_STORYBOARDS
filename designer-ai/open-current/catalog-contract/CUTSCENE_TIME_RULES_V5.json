{
  "schema": "STARWARS_DELTA_CUTSCENE_TIME_CONTRACT",
  "schemaVersion": 5,
  "packageDurationRule": "Every sequence, shot, action, transition and handoff time must remain inside package.duration.",
  "sequenceStartTime": "Absolute package time.",
  "shotStartOffset": "Local to its owning sequence.",
  "sequenceActionStartOffset": "Local to its owning sequence.",
  "shotActionStartOffset": "Local to its owning shot.",
  "pointEvents": "Events are point actions. Their startOffset may be at the owner boundary and they do not require a positive duration.",
  "formulas": {
    "shotAbsoluteStart": "sequence.startTime + shot.startOffset",
    "sequenceActionAbsoluteStart": "sequence.startTime + action.startOffset",
    "shotActionAbsoluteStart": "sequence.startTime + shot.startOffset + action.startOffset"
  },
  "authoringRule": "Do not copy package-absolute times into shot-local startOffset fields. For a shot that starts at package time 6, an event at package time 10 uses startOffset 4."
}