TASK: BUILD AND VISUALLY PROVE THE 400-SECOND STARWARS_DELTA CINEMATIC SYSTEM TEST

Canonical Unity workspace:
C:\Users\nlpis\wkspaces\STARWARS_DELTA

Read CURRENT AGENTS.md before doing anything.

This is one end-to-end production proof. It is not a concept-art exercise, a hand-built Timeline, or a request to make a pretty JSON that has not been visually proved.

The required order is:

CURRENT TRUTH -> REAL-PIXEL ASSET AUDIT -> COMPLETE 400S VISUAL STORYBOARD -> REPRESENTATIVE VERTICAL-SLICE BUILD/QA -> FULL V5/CONTEXT-V3 JSON -> UNITY MATERIALIZATION -> FRESH VISUAL QA -> STORYBOARD/RESULT COMPARISON

Do not begin final JSON authoring before the complete visual storyboard and its asset-trace manifest exist and pass preflight.

============================================================
1. WORKSPACE, SAFETY AND OWNERSHIP
============================================================

- Work directly in the canonical workspace above.
- Do not create another Unity project, workspace, clone, worktree, branch, shelf, backup project or task copy.
- The existing QA clone may be used only when isolation is useful. Never create an additional clone.
- The workspace is shared with other agents/processes. Refresh Plastic status before implementation and again before the final report.
- Reclassify the complete current pending set before any diff or synchronization. Preserve legitimate concurrent work.
- Do not check in or sync Plastic unless Haggai explicitly requests it after reviewing the proof.
- Do not change Build Profiles, Shared Scene Lists, gameplay scenes or scene mappings. Report scene-list problems to Haggai instead of repairing them yourself.
- Prefer code-first and batch/deterministic QA. Do not take over the foreground desktop or drive Unity GUI while the user is working.
- Use one inspection pass, one representative implementation/QA pass, one full batch build, one combined validation pass, then report and stop.

============================================================
2. CURRENT AUTHORING TRUTH - NO OLD SNAPSHOTS
============================================================

Use only the latest verified CURRENT publish.

Public CURRENT entrypoint:
https://streamDragon.github.io/STARWARS_DELTA_STORYBOARDS/designer-ai/open-current/OPEN_CURRENT.json

Stable full Director pack:
https://streamDragon.github.io/STARWARS_DELTA_STORYBOARDS/designer-ai/open-current/STARWARS_DELTA_CHATGPT_DIRECTOR_CURRENT.zip

Visual Atlas PDF:
https://streamDragon.github.io/STARWARS_DELTA_STORYBOARDS/designer-ai/open-current/full-visual-sheets/STARWARS_DELTA_CHATGPT_VISUAL_ATLAS_CURRENT.pdf

Single ChatGPT/authoring instruction source:
https://streamDragon.github.io/STARWARS_DELTA_STORYBOARDS/designer-ai/CHATGPT_START.txt

Film Authoring Guide:
https://streamDragon.github.io/STARWARS_DELTA_STORYBOARDS/designer-ai/FILM_AUTHORING_GUIDE_CURRENT.md

GitHub Actions machine fallback:
Workflow: Export Designer AI CURRENT artifact
Artifact name: STARWARS_DELTA_CHATGPT_DIRECTOR_CURRENT

Before selecting any ID:

1. Read OPEN_CURRENT.json.
2. Require status=CURRENT_VERIFIED_OPEN.
3. Verify that publishTransactionId, catalogRevision, snapshotContentHash, contractRevision and schemaHash agree across OPEN_CURRENT, Director View, Catalog contract and fallback pack.
4. Read CHATGPT_START.txt, Film Authoring Guide, Instruction Book and the relevant Director category files.
5. Never use remembered IDs, old JSONs, old Catalog snapshots or assets from another publish transaction.

If the atomic identity does not agree, stop and report the exact mismatch. Do not silently combine generations.

============================================================
3. EXISTING PRODUCTION FIXES TO VERIFY, NOT BLINDLY REWRITE
============================================================

The current production branch is expected to contain these recent fixes:

- MY_CutsceneLayoutPolicy.cs
  - Full-frame background fitting includes both endpoints of nested V5 camera motion.
- MY_CutsceneTimelineWriters.cs
  - All background roles receive coverage fitting.
  - Renderer sorting preserves prefab-relative order.
  - Missing scripts are stripped after safe prefab instantiation.
- MY_CutsceneGenerator.cs
  - Screen-basis occupancy metrics are correct.
  - Actor/VFX sorting is deterministic.
  - VFX materialization is safer.
- CutsceneRenderingReliabilityTests.cs
  - Camera-motion coverage and sorting regressions are covered.

Inspect the current files and confirm the behavior exists before changing them.

Unity recompilation was previously reported as PASS, but the focused test report was stale and no fresh visual QA was completed. Therefore:

- Do not reimplement these fixes without a concrete current failure.
- Do not claim focused-test success from the stale report.
- This task must produce fresh deterministic test evidence and fresh visual evidence.

============================================================
4. CLOSED-WORLD VISUAL AUTHORING - ABSOLUTE RULE
============================================================

Everything visible in the storyboard and final cutscene must come from the verified CURRENT packages or the canonical Unity project assets they identify.

Forbidden:

- image generation used as storyboard content;
- invented ships, robots, rooms, planets, weapons, portraits, VFX or UI;
- generic placeholder silhouettes presented as real assets;
- selecting an asset only from its filename, tag or displayName;
- copying a Visual Atlas/preview/sprite ID into an Actor field without proving runtime legality;
- claiming VISUAL_VERIFIED when pixels were not actually inspected.

Black-and-white storyboard treatment is allowed only by transforming real project pixels, for example grayscale, threshold, edge treatment or monochrome compositing. It must remain traceable to the real source asset. Do not redraw or replace the asset with an imagined sketch.

For every important storyboard element preserve this complete chain:

OBSERVED APPEARANCE -> ATLAS PAGE/SLOT OR UNITY SOURCE PREVIEW -> EXACT VISUAL EVIDENCE ID -> EXACT CANONICAL RUNTIME/AUTHORING ID -> LEGAL ROLE/CAPABILITY

A visual reference is not automatically a runtime Actor.

- World/cast Actor must resolve to a canonical CURRENT asset with Cutscene.Actor and the required role capability.
- Ship Actor must satisfy the required Actor/Ship capability and runtime form.
- Layer/background must be legal in a Layer destination.
- Effect must be legal in an Effect destination.
- UI/portrait/dialogue frame must be legal in the correct UI or dialogue field.
- PlayAnimation may use only an exact animationAssetId explicitly listed as compatible for that canonical Actor.
- Audio must use exact CURRENT Audio IDs.

If actual pixels cannot be inspected through the available tool path, say so immediately in the task log. Do not spend several steps pretending metadata is vision. Use the local Unity source assets or the single CURRENT Atlas PDF. If neither is available, stop visual selection and report the exact missing source.

============================================================
5. GOAL: ONE EXACTLY 400-SECOND CINEMATIC PROOF
============================================================

Working title:
STARWARS_DELTA - THE ASTEROID VEIL - 400S CINEMATIC SYSTEM PROOF

The film must be exactly 400.0 seconds from first frame to handoff.

The dramatic spine must include the original required event:

A group of small ships patrols and visibly rotates/turns through space. A wave of asteroids crosses the lane. A much larger mothership arrives. The smaller ships recognize it, break formation and flee. This action is intercut with characters, face closeups, cockpit/communications and command-room dialogue. The story then escalates into a counterlaunch, gunfire, impacts, VFX, robot/character animation, tactical UI and a cinematic aftermath.

Use this sequence timing as the structural target. Small timing adjustments are allowed only if the exact total remains 400.0 seconds and the dramatic progression is preserved.

1. 000-036 - COLD PATROL
   Small fighters cross a readable space environment in formation. Show at least one real ship rotation/turn and one formation movement. Establish engine ambience and restrained camera motion.

2. 036-078 - ASTEROID WAKE
   A layered asteroid wave enters from depth and crosses the patrol. Fighters evade, bank, turn and separate. Use different asteroid/rock visuals only when their real pixels and legal runtime IDs are proved.

3. 078-116 - COMMAND DETECTION
   Cut to a real command-room/control-room visual. Use tactical UI/radar/monitor inserts, beeps and two dialogue participants. Include at least two face closeups and one reaction shot.

4. 116-158 - MOTHERSHIP REVEAL
   Return to space. Reveal the large ship with motivated camera language, not arbitrary scaling. The mothership must read as materially larger than the patrol ships. Use Pull/Push/Reveal/Orbit/Dolly intent only where supported.

5. 158-202 - BREAK FORMATION
   The small ships flee before or as the mothership enters their lane. Each exit must be visually readable. Include different escape vectors, turns/rotation, engine audio and command dialogue over communications.

6. 202-248 - COUNTERLAUNCH
   Command orders a response. Show a launch/deployment beat using verified ships, robots or characters and exact compatible animations. Include cockpit/monitor UI, warning beeps and a clear location transition.

7. 248-300 - CROSSFIRE
   Sustained cinematic action: lasers/projectiles, muzzle/engine effects, impacts, explosions, near misses and camera recoil/shake where legal. Maintain readable depth and avoid visual clutter. Audio must include distinct weapon, impact and explosion categories when CURRENT provides them.

8. 300-344 - CLOSE ACTION
   Use verified character/robot/ship animation families to test exact compatibility. Include at least one locomotion/deployment action, one attack/fire action and one damage/reaction action when compatible clips exist. Do not fake missing animations.

9. 344-378 - TACTICAL REVERSAL
   Return to command room and tactical screens. Use closeups, dialogue, a decision beat and an external payoff shot. Include a short moment of intentional silence or reduced ambience before the reversal.

10. 378-400 - AFTERMATH
    End with a readable wide composition, debris/atmosphere where appropriate, a final reaction or communication beat, and a clean typed handoff.

Do not turn these into ten equal mechanical blocks internally. Shot duration must follow dramatic purpose.

============================================================
6. SHOT AND FEATURE COVERAGE
============================================================

Target 48-56 shots total. Use variable durations and motivated cuts.

The completed film must visibly exercise as many supported production systems as possible without sacrificing readability:

CAMERA / EDITING
- Static composed Hold
- Push in
- Pull back / scale reveal
- Pan
- Track or Follow
- Target-group framing when supported
- Orbit or orbital composition when supported
- Spline/Dolly movement when supported
- Hard cuts
- At least one fade or other legal transition
- Impact/recoil/shake as additive support, never as the only camera action
- At least six character/portrait closeups or reaction framings

SUBJECT MOTION
- Enter frame
- Exit frame
- Lateral pass
- Approach and recede
- Formation movement
- Visible turn/rotation
- Evasive movement
- Flee/scatter movement
- One large-ship scale reveal

ROTATION REQUIREMENT
Inspect the current Authoring Contract and production action vocabulary.

- If a real supported RotationAnimation/Rotate/orientation action exists, use its exact canonical name and fields.
- Otherwise use the legal production route, such as Turn, authored rotation values/keyframes, or a V3 semantic orbit plan.
- Never invent an action enum because its name sounds plausible.
- Prove at least one ship changes its own visible orientation and at least one camera performs an orbit/orbital composition if supported.

ANIMATION
- Use exact compatibleAnimationIds only.
- Cover at least three distinct canonical Actor identities when the CURRENT compatibility data supports them.
- Prefer representative actions such as launch/deploy, walk/run/move, shoot/attack, shield/defend, damage/hit and deactivate only when exact compatibility proves them.
- If a desired action has no compatible clip, use a legal non-animation movement/pose or simplify the beat. Do not assign a similarly named clip from another Actor.

VISUAL CATEGORIES
Use a coherent selection from:
- ships and other world Actors;
- characters/robots;
- asteroids/props;
- multiple environment/layer families;
- VFX/effects;
- portraits/dialogue frames;
- tactical UI/radar/monitors;
- animation representative families;
- audio.

Do not chase a raw asset count by placing random incompatible art in the frame. Variety must remain cinematic and spatially coherent.

AUDIO
Perform a real Audio pass using exact CURRENT IDs. Test as many relevant categories as CURRENT safely provides:
- space/room ambience;
- engines/movement;
- cockpit or tactical beeps;
- alert/siren;
- laser/weapon fire;
- impacts;
- explosions;
- UI confirmation;
- music or tonal bed when suitable;
- intentional silence/reduction.

Audio is non-visual and must not be treated as a visual-coverage failure.

DIALOGUE
- Use real Actor-primary cast identities and compatible Ui-primary portrait/frame assets.
- Locked dialogue must have shot.actorActions=[].
- Locked dialogue camera must be Hold or cut-based Snap according to the current contract.
- FACE_TO_FACE_PORTRAITS participants must be dialogue-only with spawnWorldActor=false unless the current presentationMode contract explicitly permits a temporally isolated Both route.
- Do not use the same active cast entry simultaneously as a world Actor and locked portrait participant.
- Use only one primary visual source per participant: portrait or body, not both.
- SUBTITLE_ONLY must not contain portraits.
- Include command-room dialogue, cockpit/communication dialogue, urgent reaction and quiet aftermath delivery.

============================================================
7. STORYBOARD FIRST - REQUIRED DELIVERABLE AND GATE
============================================================

Before final JSON authoring, create the complete visual storyboard for all 400 seconds.

The storyboard must be an actual visual composition made from real CURRENT/Unity asset pixels. A text-only shot list, blueprint, boxes with labels or generated concept art does not satisfy this requirement.

Recommended format:
- 6 panels per page;
- 8-10 pages depending on final shot count;
- 16:9 panel frames;
- monochrome is acceptable when derived from the real pixels;
- preserve enough contrast to identify the selected asset.

Every panel must visibly label:
- sequence ID and shot ID;
- absolute start time and duration;
- shot type/framing;
- camera intent and transition;
- key subject names;
- dialogue line summary when present;
- exact visual evidence IDs;
- exact canonical runtime IDs;
- Atlas page/slot or local Unity proof source;
- expected animation/audio/VFX beats.

Create these files before JSON:

1. STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_STORYBOARD.pdf
2. STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_STORYBOARD_PAGES.zip
3. STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_STORYBOARD_MANIFEST.json
4. STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_ASSET_TRACE.json
5. STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_SHOT_PLAN.json

The manifest must include:
- exact CURRENT atomic identity;
- expected and actual shot/page counts;
- exact total duration=400.0;
- page filenames and covered shot ranges;
- status SUCCESS only when all shots have real visual panels;
- explicit list of any asset that is metadata-verified but not pixel-verified.

STORYBOARD PREFLIGHT

Do not pass the storyboard gate unless:
- all 400 seconds and every shot are represented;
- every principal visible asset was actually inspected;
- no concept/image-generated art is used as Catalog evidence;
- visual evidence IDs map to legal canonical runtime IDs;
- location/style/perspective continuity is intentional;
- the large ship reads larger than the small ships for semantic reasons, not arbitrary source-size scaling;
- dialogue participants and backgrounds are visually traceable;
- storyboard timings sum exactly to 400.0 seconds.

If the storyboard gate fails, fix it before writing the final V5 package.

============================================================
8. REPRESENTATIVE VERTICAL SLICE BEFORE FULL MATERIALIZATION
============================================================

After the complete storyboard passes, create and build one 45-60 second representative slice from it before generating/materializing the full 400-second film.

The slice must include:
- one full-frame environment with camera motion;
- one ship turn/rotation;
- one asteroid movement beat;
- one locked dialogue beat with face closeup;
- one laser/impact/VFX beat;
- one Audio ambience/beep/weapon combination;
- one transition;
- one handoff or deterministic end state.

Use this slice to verify the production path, not a separate hand-made scene.

Run focused deterministic/EditMode tests and fresh visual capture for the slice. If it exposes a real production defect:

- fix the production generator/planner/materializer path;
- add or update a focused regression test;
- rebuild the same slice;
- do not hide the failure by weakening the storyboard, changing to a random asset, adding a diagnostic placeholder, or hand-editing the generated Timeline.

Once the representative slice is visually acceptable, generalize the proven pattern to the full 400-second package in one batch.

============================================================
9. JSON OUTPUTS - CURRENT PRODUCTION FORMAT FIRST
============================================================

PRIMARY PRODUCTION PACKAGE

Create one complete production package:

STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_V5_CONTEXT_V3.json

It must:
- use schema STARWARS_DELTA_CUTSCENE_PACKAGE;
- use schemaVersion=5;
- use contextVersion=3;
- use the exact CURRENT contractRevision, schemaHash and catalogRevision;
- have duration=400.0;
- use globally unique technical IDs;
- use exact CURRENT asset IDs only;
- preserve storyboard shot IDs and timing;
- include a typed handoff at 400.0 seconds;
- validate with zero schema, semantic, capability, compatibility and timing errors.

V3 DIRECTOR INTENT

Use the current production V3 semantic extension/planner route only if it is exported and supported by the current contract/code.

- Prefer semantic camera intent: shot size, angle, composition, movement, subject, purpose, intensity and transition.
- Let the V3 Cinemachine planner/materializer translate supported intent.
- Do not expose or invent raw Cinemachine parameters in authoring JSON.
- Locked dialogue remains static Hold or cut-based Snap.
- If the current production route embeds V3 intent as an extension, use that route.
- If it requires a separate sidecar, create:
  STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_V3_INTENT.json
- Do not invent an unsupported V3 schema.

V2 MIGRATION PROOF

V5/contextVersion 3 is the production source of truth. V2 is not a second competing final film.

If the current migration system still supports V2 input, create one representative migration fixture derived from the approved storyboard slice:

STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_V2_MIGRATION_FIXTURE.json

Then prove that the production migration/recovery path converts it deterministically and reports all changes. Do not maintain two independently authored 400-second films.

STORYBOARD/JSON TRACE

Also create:
- STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_STORYBOARD_TO_JSON_MAP.json
- STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_EXPECTED_CAPTURES.json

The mapping must link each storyboard panel/shot to:
- sequenceId;
- shotId;
- JSON timing;
- camera action IDs;
- layer action IDs;
- actor action IDs;
- dialogue line IDs;
- effect IDs;
- audio IDs;
- expected capture timestamps.

============================================================
10. PRODUCTION MATERIALIZATION AND VALIDATION
============================================================

Build through the normal Cutscene Studio / production compiler, planner, materializer and Timeline writers. Do not hand-build a special Timeline or scene that bypasses production.

Run one combined final technical validation pass after the representative pattern has been generalized.

At minimum verify:

CONTRACT / ASSETS
- exact atomic CURRENT identity;
- schema and semantic validation;
- every Actor has legal Cutscene.Actor capability where required;
- visual references are not used as runtime Actors;
- every Animation is exactly compatible with its Actor;
- every Layer/Effect/UI/Audio ID is in a legal destination;
- no unsupported enum/action aliases;
- no gameplay event leakage;
- typed handoff is valid.

TIMELINE / LIFETIME
- authored and generated shot counts agree;
- camera base/additive clips exist as expected;
- layer tracks/actions and roles exist as expected;
- actor actions and animation clips exist as expected;
- dialogue presenters/lines/presets exist as expected;
- transitions, VFX and audio clips exist as expected;
- no temporal leaks between locations/sequences;
- no duplicate active camera;
- no duplicate world Actor for dialogue-only participants.

RENDERING RELIABILITY
- full-frame backgrounds cover at both endpoints of camera motion;
- all background roles receive coverage fitting;
- actor/VFX sorting is deterministic;
- prefab-relative renderer order is preserved;
- missing scripts are stripped from safe cinematic instances;
- principal materials/shaders are valid;
- no magenta principal visuals;
- no diagnostic placeholder is accepted as a principal result;
- occupancy/proportions are sane in screen space;
- large/small ship relationship is semantically readable;
- UI remains within safe screen bounds.

FOCUSED TESTS
Run the currently relevant focused suites, including the actual current classes where present:
- CutsceneRenderingReliabilityTests
- CutsceneCameraStateTests
- CutsceneTimelineWriterIntegrationTests / Timeline architecture tests
- CutsceneAssetResolutionTruthTests
- CutsceneSchemaV5Tests
- CutsceneCreativeContextV3Tests
- V3 Cinemachine planner tests
- V3 spatial staging tests
- V3 learning/dialogue invariant tests

Do not claim PASS from an old XML/report. Require a fresh timestamped result generated in this run.

COMPILE
- Unity Editor compilation must finish with zero new compiler errors.
- One compile after the implementation batch, not repeated after every asset.

============================================================
11. FRESH VISUAL QA AND STORYBOARD COMPARISON
============================================================

This task is not complete at JSON validation or successful import.

Run fresh visual QA in the canonical workspace or existing QA clone. Do not create another project copy.

Capture enough actual Game View frames to compare the final result to the storyboard:
- at least start/middle/end of every sequence;
- every major location transition;
- every dialogue closeup/reaction type;
- the asteroid wave;
- the mothership scale reveal;
- the moment the small ships flee;
- representative laser/VFX/impact beats;
- representative robot/character animation beats;
- final aftermath/handoff.

Produce:

1. STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_VISUAL_QA.zip
2. REPORT.md
3. SUMMARY.csv
4. one contact sheet per sequence;
5. original PNG captures;
6. STORYBOARD_COMPARISON.md

The comparison must pair storyboard panel and generated capture and classify each as:
- MATCH;
- ACCEPTABLE VARIATION;
- MISMATCH;
- BLOCKED / NOT CAPTURED.

For every mismatch report the concrete reason, for example:
- wrong asset identity;
- wrong runtime/canonical mapping;
- background coverage failure;
- wrong size/proportion;
- offscreen target;
- camera intent not materialized;
- animation missing/incompatible;
- VFX/audio missing;
- sorting/overlap problem;
- dialogue framing mismatch;
- temporal leak;
- magenta/material failure;
- diagnostic placeholder visible.

Do not claim visual success unless the fresh captures exist and the comparison was performed.

If foreground visual QA cannot be run safely while the user is active, perform all deterministic/batch QA and provide one short manual capture checklist. Mark visual acceptance PENDING rather than inventing success.

============================================================
12. OUTPUT LOCATION AND FINAL DELIVERY
============================================================

Place final human-facing deliverables under:

C:\Users\nlpis\Downloads\STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF\

Create one final ZIP:

STARWARS_DELTA_400S_CINEMATIC_SYSTEM_PROOF_CURRENT.zip

The ZIP must contain the storyboard, manifests, asset trace, shot plan, production V5/context-V3 JSON, supported V3 intent data, V2 migration fixture/result when applicable, expected-capture map, validation reports and visual QA evidence.

Do not include Unity Library/Temp/cache junk, credentials, tokens, unrelated pending files or giant source-art archives.

============================================================
13. FINAL REPORT FORMAT
============================================================

Return one concise but complete final report with these headings:

1. CURRENT atomic identity used
2. Storyboard result and exact files
3. Real-pixel asset coverage and any unverified exclusions
4. Story/sequence/shot summary and exact 400.0s timing
5. Representative vertical-slice result
6. Production code defects found and exact files changed
7. Primary V5/context-V3 JSON result
8. V3 intent route used
9. V2 migration proof result
10. Compile and fresh focused-test results
11. Visual QA capture result
12. Storyboard-versus-Unity comparison summary
13. Remaining blockers or mismatches
14. Plastic status and confirmation that no check-in was performed
15. Final ZIP path

Do not start unrelated Catalog rescans, Vision batches, scene archaeology, multiplayer work, gameplay refactors or broad tool redesigns in this task.

Finish the 400-second proof, report honestly, and stop.
