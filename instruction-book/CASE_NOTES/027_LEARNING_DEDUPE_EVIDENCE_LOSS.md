# CASE 027 - Learning dedupe must preserve distinct occurrences

Two dialogue lines can fail the same rule. Stable issue identity includes path/entity/line context so both remain evidence while true duplicate validator reports are collapsed.
