# Mars Cafe homecoming: immutable BAD evidence and GOLDEN gate

Case ID: `MARS_CAFE_HOMECOMING_V1_BAD`

This case is preserved as raw regression evidence from the first 60-second Mars Cafe homecoming proof. It is intentionally BAD and must remain immutable. The machine-readable fixture is:

`Assets/_Game/MY_Core/MY_Scripts/HAGGAI_STUFF/Editor/Tests/Fixtures/MARS_CAFE_HOMECOMING_V1_BAD.json`

The evidence records the failures that must not silently return:

- a postage-stamp background or black frame borders;
- yellow diagnostic placeholders for required principal ships;
- a system-managed actor outside the semantic screen-space band;
- role/capability mismatch accepted before preview build;
- authored scale used as source-size compensation;
- incompatible top-down/pixel ships in an eye-level painterly cafe world;
- omitted audio pass;
- mechanically equal shot rhythm; and
- an authored Effect without materialization proof.

Regression tests must use the normal Studio import/materialization path. A required principal fallback is RED or blocked, never normal GREEN. A required Effect without evidence is explicitly degraded or blocked. Background coverage must remain at least 95% through representative camera motion, and system-managed actors use authored scale 1.0 as their semantic baseline.

Do not create a corrected Mars Cafe JSON in this case and do not add it to `InstructionBook/GOLDEN`. A future corrected artifact must have a separate identity/version and may be promoted only after WP1-WP3 automated invariants pass and a manual canonical visual review confirms:

`war over -> ships home -> new home on Mars`

This note teaches the reusable gate; the raw failure evidence remains the source of truth for this regression.
