# CASE 022 - Learning cases are versioned evidence

## Observed
The inbox can contain cases authored under older contract revisions alongside current failures. Aggressive issue dedupe can also hide distinct blockers.

## GOOD
Preserve every Error issue. Store sourceContractRevision/sourceSchemaHash, normalized identities and current contract identity separately. Raw cases never automatically become active lessons.
