(async()=>{const grid=document.getElementById('grid');try{const r=await fetch('storyboards/index.json',{cache:'no-store'});const items=await r.json();grid.innerHTML=items.map(x=>`<a class="card" href="viewer.html?id=${encodeURIComponent(x.id)}"><img src="${x.cover}" alt="${x.title}"><div class="cardbody"><div class="verified">${x.status}</div><h2>${x.title}</h2><div class="meta"><span>${x.duration}s</span><span>${x.shots} shots</span></div><p class="summary">${x.summary}</p></div></a>`).join('')||'<div class="loading">No storyboards published yet.</div>'}catch(e){grid.innerHTML='<div class="loading">Could not load storyboard index.</div>'}})();
